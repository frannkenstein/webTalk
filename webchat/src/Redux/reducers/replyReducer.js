import { Reply } from "../constants/constantsTypes";

var STATE = {
  reply: [],
};

export const myReply = (state = STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
